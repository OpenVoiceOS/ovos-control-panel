name: Code Coverage

on:
  pull_request:
    branches: [dev]
  workflow_dispatch:

jobs:
  coverage:
    uses: OpenVoiceOS/gh-automations/.github/workflows/coverage.yml@82252c363b239fe5c60dc0ae2a4e81721d4cabef
    with:
      python_version: '3.11'
      coverage_source: 'ovos_webui'
      test_path: 'tests/'
      install_extras: '.[dev]'
      # ggwave (a test dependency) pulls in PyAudio, which needs portaudio to build.
      system_deps: 'portaudio19-dev'
      min_coverage: 0
