name: Build Tests

on:
  pull_request:
    branches: [dev, master, main]
  workflow_dispatch:

jobs:
  build:
    uses: OpenVoiceOS/gh-automations/.github/workflows/build-tests.yml@82252c363b239fe5c60dc0ae2a4e81721d4cabef
    with:
      python_versions: '["3.10", "3.11", "3.12", "3.13", "3.14"]'
      install_extras: 'dev'
      # ggwave (a test dependency) pulls in PyAudio, which needs portaudio to build.
      apt_packages: 'portaudio19-dev'
      test_path: 'tests'
